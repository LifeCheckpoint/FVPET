[package]
name = "rfvp-cli"
version = "0.1.0"
edition = "2021"
description = "Headless rfvp runner: boots a compiled HCB and speaks the hcb-editor stdio JSON protocol"

[dependencies]
rfvp = { path = "../../vendor/rfvp/crates/rfvp", default-features = false, features = ["runtime-core-deps", "zlib-flate2"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
